import { TestServiceDirective } from './test-service.directive';

describe('TestServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new TestServiceDirective();
    expect(directive).toBeTruthy();
  });
});
